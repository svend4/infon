# Command index — all 121 commands

_Auto-generated by `go run scripts/gen_command_index.go` from each command's
package doc comment — do not edit by hand. For fuller usage of one command run
`go doc ./cmd/<name>`; for a curated quick start and the ray/Q6/alife platform see
[COMMANDS.md](COMMANDS.md); for what the project is and how it composes see
[CONCEPT_MAP.md](CONCEPT_MAP.md)._

| Command | What it is | Run |
|---|---|---|
| `aclnegotiate` | Shows multiagent NEGOTIATION (pkg/acl, Contract Net): an initiator calls for proposals on a task, agents bid or refuse, and the best bid is awarded — the conversation printed pe… | `go run ./cmd/aclnegotiate` |
| `aicam` | Streams an AI video source. | `go run ./cmd/aicam` |
| `aicards` | Renders a glyph "card" message + a truecolor effect via pkg/terminal.Frame. | `go run ./cmd/aicards` |
| `aidraw` | Renders to the terminal via pkg/scene. | `go run ./cmd/aidraw` |
| `aiimg` | Renders a raster image into the terminal via the repo's own internal/codec/babe.ImageToFrame. | `go run ./cmd/aiimg assets/aurora.png 64 40` |
| `aiplay` | Connects an external "brain" to the repo's tic-tac-toe engine (experimental/games/board) and renders every board as a colored Frame. | `go run -tags experimental ./cmd/aiplay` |
| `aiplayf` | A LIVE tic-tac-toe driven over a shared folder, so an external brain (Claude) can play move-by-move while the repo engine + renderer run for real. | `go run -tags experimental ./cmd/aiplayf` |
| `aiplayi` | An INTERACTIVE tic-tac-toe: X reads moves from stdin (so a live LLM can play through the terminal via interact_with_process), O is the repo engine (random here for a winnable game). | `go run -tags experimental ./cmd/aiplayi` |
| `aiturn` | Plays ONE turn per run against the repo engine, persisting the game in a shared folder so an external brain (you, or another model) drives X move-by-move. | `go run ./cmd/aiturn` |
| `aiuno` | Plays UNO where a tvcp-ai/1 brain controls one player, using the repo's experimental/games/cards engine. | `go run -tags experimental ./cmd/aiuno -brain http://127.0.0.1:8088/v1/decide` |
| `aiwordle` | Plays Wordle where a tvcp-ai/1 brain guesses, using the repo's experimental/games/words engine for validation/feedback. | `go run -tags experimental ./cmd/aiwordle -brain http://127.0.0.1:8088/v1/decide -word WATER` |
| `anchordemo` | A hand-made bridge to the MDLM world-model paper (Patronus AI, 2026): it shows, on our symbolic grid, why ANY-ORDER decoding with ANCHORS beats strict LEFT-TO-RIGHT. | `go run ./cmd/anchordemo` |
| `anyorderdemo` | Shows the MDLM "any-order with anchors" idea on the tangram address book: to identify a figure, reveal the most informative cell next (any-order) instead of scanning left-to-right… | `go run ./cmd/anyorderdemo` |
| `areciboframe` | Renders the full Lingua-Cosmica plaque (pkg/arecibo): one frame that teaches its own alphabet. | `go run ./cmd/areciboframe -fig cat` |
| `arena` | Runs an "origami Warcraft" skirmish: two factions of tangram units, each driven by the reference Commander, charge across an isometric terrain board. | `go run ./cmd/arena` |
| `arenaai` | Runs the origami-Warcraft skirmish with each side commanded by a tvcp-ai brain (game "rpg"): by default the reference commander on both, or a live model per side via -brain0 / -brain1. | `go run ./cmd/arenaai` |
| `arenacall` | The project's "three bodies into one core" demo: a live arena match (the agent-world) is rendered as marks pseudo-images and streamed through pkg/semvideo — the very codec TVCP… | `go run ./cmd/arenacall` |
| `arenacast` | Broadcasts an arena match as a loss-tolerant semantic stream and renders the SPECTATOR's view: keyframe+delta packets over Reed-Solomon are dropped and corrupted on a lossy channel,… | `go run ./cmd/arenacast` |
| `arenalive` | The capstone: a LIVE war of two models, broadcast over a lossy channel to a spectator. | `go run ./cmd/arenalive` |
| `arenammo` | The capstone of the origami-MMORPG: armies are SUMMONED from tangram figures, each side is commanded by a LIVE tvcp-ai brain (-brain0/-brain1), the match is broadcast as RS packets… | `go run ./cmd/arenammo` |
| `arenaplan` | Pits the lookahead Planner (blue) against the reactive RefCommander (red) on a symmetric board with a central ridge, prints the tick-by-tick score, and renders a top-down GIF (via pk… | `go run ./cmd/arenaplan` |
| `arenaserver` | A persistent tick-server with multiple networked spectators: it runs an arena match and broadcasts each tick as an RS-protected packet over UDP (real localhost loopback) to seve… | `go run ./cmd/arenaserver` |
| `arenasummon` | Demonstrates crafting: a tangram figure is classified and, if it names a creature in the bestiary, a unit is summoned into the arena - "the address is the summoning spell". | `go run ./cmd/arenasummon` |
| `arenatale` | Plays an arena match, distils it into a few-byte chronicle (pkg/chronicle), and retells that byte-log as prose — the project's chain number -> image -> world -> story, end to end. | `go run ./cmd/arenatale` |
| `autodemo` | Shows the auto triangle/quadrant chooser (pkg/automode). | `go run ./cmd/autodemo` |
| `blazondemo` | Renders example coats of arms (heraldic blazons) to PNGs, proving that a medieval visual-identity language compiles straight into the pseudo-image pipeline. | `go run ./cmd/blazondemo [outdir]` |
| `bmeaning` | Demonstrates bidirectional B-frames of meaning (pkg/semvideo): a constant-shape marks "video" is coded as anchors (keyframe + P-frames) with B-frames in between, each predicted from t… | `go run ./cmd/bmeaning` |
| `braindemo` | Proves the open tvcp-ai/1 format end-to-end over real HTTP: it starts the reference brain on localhost, then (1) plays a full tic-tac-toe where X's moves come from the brain via the… | `go run ./cmd/braindemo` |
| `brainserver` | Serves the reference tvcp-ai/1 brain over HTTP, so any client (game, draw) can use it. | `go run ./cmd/brainserver` |
| `capsdemo` | Shows tvcp-ai/1 capability negotiation (pkg/brain): a brain serves /v1/capabilities and /v1/decide on one endpoint; a client fetches the capabilities and checks support before sending… | `go run ./cmd/capsdemo` |
| `conform` | Checks whether a tvcp-ai brain satisfies the protocol contract. | `go run ./cmd/conform` |
| `connect4demo` | Shows a SECOND game on the same session runner (pkg/session): Connect Four, a win/block tactician bot against a naive leftmost bot. | `go run ./cmd/connect4demo` |
| `debate` | Stages a judged AI debate (pkg/debate): two debaters argue a topic over several rounds and a judge scores each one. | `go run ./cmd/debate` |
| `dtncast` | Streams an arena match over a DELAY-TOLERANT (deep-space) link (pkg/dtn over pkg/semvideo): the world's frames become bundles forwarded only during a few short contact passes, with a t… | `go run ./cmd/dtncast` |
| `foldcall` | Sends an origami unfold of a tangram figure as a stream of per-piece fold angles (keyframe + deltas) over the Reed-Solomon channel, then corrupts a burst of wire bytes, recovers, and… | `go run ./cmd/foldcall` |
| `folddemo` | Folds flat tangram7 figures into a pseudo-3D isometric relief: it writes a hero still and an animated GIF of the figure rising out of the plane, and prints the tiny FoldSpec that desc… | `go run ./cmd/folddemo` |
| `glyphdemo` | Renders the digitized alphabet chart, and compares the quadrant codec with the new diagonal/triangle codec on the same image. | `go run ./cmd/glyphdemo [outdir]` |
| `glyphqrdemo` | Shows the glyph-QR with Reed-Solomon error correction: encode a message into a glyph grid, damage several cells, and watch it decode back to the exact text — the self-correcting… | `go run ./cmd/glyphqrdemo [outdir]` |
| `glyphqrscan` | Demonstrates the optical glyph-QR: a framed code with finder marks in three corners decodes from ANY rotation. | `go run ./cmd/glyphqrscan` |
| `imagine` | The "protocol of shared imagination": one agent-world is broadcast ONCE as a semantic-video stream (pkg/arenacast + pkg/semvideo), and TWO independent receivers — with their own p… | `go run ./cmd/imagine` |
| `linguaframe` | Builds a self-describing "Lingua Cosmica" frame for a tangram figure and then decodes it back using nothing but the bytes — the receiver learns to count, then learns the radix, t… | `go run ./cmd/linguaframe` |
| `orbit3d` | Turns the pseudo-3D world into a turntable: it renders a relief voxel cat across a full 360-degree camera-yaw sweep (fold.RenderCam) and ships the camera path as a one-byte-per-frame d… | `go run ./cmd/orbit3d` |
| `paint` | Shows the on-device painter (pkg/painter): with no model and no network it reads a prompt into a pseudo-image grid, which the deterministic pseudo-diffusion renderer turns into a painter… | `go run ./cmd/paint` |
| `planbrain` | Serves the arena lookahead Planner as a tvcp-ai/1 endpoint, so any client can point -brain at it by URL. | `go run ./cmd/planbrain -addr 127.0.0.1:8099` |
| `pseudo3d` | The synthesis of the three pseudo-3D paths in one scene and one stream. | `go run ./cmd/pseudo3d` |
| `pseudodemo` | Renders the same scene ("a calm harbor at dawn") in every pseudo-image format, proving that an ordinary text model — one that can only emit JSON, no diffusion head — can paint p… | `go run ./cmd/pseudodemo [outdir]` |
| `ray3d` | Renders a small CPU ray-traced scene — spheres on a checkerboard floor with Lambert/Blinn-Phong shading, hard shadows, a mirror and glass — into the terminal using truecolor glyph mo… | `go run ./cmd/ray3d` |
| `rayagent` | Puts the robots under a tvcp-ai/1 brain: whenever a robot is ready for a new destination the brain picks one of its stations, given the robot's position and status. | `go run ./cmd/rayagent` |
| `rayarena` | Stages a 3-D "arena": a tvcp-ai brain authors a moving world (orbiting, breathing spheres) AND flies the camera, the whole evolving scene is broadcast as independent Reed-Solomon pack… | `go run ./cmd/rayarena` |
| `rayask` | Asks a brain to author a world from a prompt and path-traces it. | `go run ./cmd/rayask "a forest at night"` |
| `raybench` | Evaluates a rayscene director (the reference brain, or a live model via -url): it asks for many scenes and reports how renderable, rich and varied they are — a quick objective read… | `go run ./cmd/raybench` |
| `raycall` | The semantic video call, end to end, over a real (loopback) UDP socket: it joins the pieces this project built separately — | `go run ./cmd/raycall -frames 48` |
| `raycamp` | Runs a robot logistics yard — Part 1's "logistics in a camp". | `go run ./cmd/raycamp` |
| `rayclimate` | Visualises the hexagram-cellular-automaton climate (block: hexca -> weather). | `go run ./cmd/rayclimate -seed 42` |
| `raycurator` | A curator agent that searches the Q6 hypercube for the world a viewer loves MOST (Block K: curator = engagement × maze × agent). | `go run ./cmd/raycurator -seed 16 -walls 0.5 -taste 110010` |
| `raydebate` | Stages adversarial co-direction (the "debate × director" combination): two brains each author a region for a prompt, the rayscene validator referees, and the richer valid region win… | `go run ./cmd/raydebate "a temple by water"` |
| `raydetect` | Closes the semantic-video chain: a vision model's detections become a rayscene, which is then path-traced. | `go run ./cmd/raydetect` |
| `raydirect` | Shows meaning-driven direction in both directions (Block C). | `go run ./cmd/raydirect -seed 3 -rounds 250` |
| `raydungeon` | Plays the Q6 maze as a LIVING dungeon (Block E: alife × rogue). | `go run ./cmd/raydungeon -seed 16 -walls 0.6 -ticks 200` |
| `rayexplore` | The experience: you walk, in the terminal, through a 3-D world the AI director dreams up and extends on the fly. | `go run ./cmd/rayexplore` |
| `rayface` | Renders a meeting INSIDE the world: participant avatars whose faces are drawn from keypoints (raydir.AvatarFace — the landmarks internal/avatar sends ~35 kbps over the wire) stand to… | `go run ./cmd/rayface` |
| `rayfilm` | Flies a cinematic camera through a brain-authored world and prints a contact sheet of the shots. | `go run ./cmd/rayfilm -frames 9 -cols 3 -path -grade -out film.png` |
| `rayfleet` | Watches a fleet of machines the way info150's triage4-drive watches a driver: it ingests per-unit signal readings, fuses them into a severity, tracks each unit against its own baselin… | `go run ./cmd/rayfleet` |
| `rayfx` | Activates three renderers that shipped in pkg/raytrace fully tested but wired to no command (the "dormant code" of the audit): photon-mapped caustics, adaptive sampling, and temporal rep… | `go run ./cmd/rayfx -mode caustics` |
| `raygallery` | Browses a directory of saved worlds (.rwld) and recordings (.rrec), printing a summary of each and how to open it — a little gallery of shareable worlds and walks, each a few KB o… | `go run ./cmd/raygallery worlds/` |
| `raygates` | Runs the sim-to-real conformance gates — Part 1's variant E. | `go run ./cmd/raygates` |
| `raygather` | A meeting INSIDE a shared world (Block A: metaverse): N participants, each an avatar with a face (raydir.AvatarFace from keypoints), sit in a circle in a hexagram-authored world a… | `go run ./cmd/raygather -n 5 -hexagram 110010` |
| `rayhard` | Shows the integrator "renderer brain" (block L): on a caustic dream scene where the unidirectional path tracer is noisy, RenderBest probes the difficulty and switches to the bidirectio… | `go run ./cmd/rayhard` |
| `rayimagine` | Reads a picture and renders the 3-D world it implies — the project's idea in reverse: meaning extracted FROM pixels. | `go run ./cmd/rayimagine photo.png` |
| `raylife` | Runs a small artificial-life world that lives WITHOUT a player (Block B: alife). | `go run ./cmd/raylife -seed 7 -ticks 240` |
| `raymandala` | Activates raydir.Mandala — dihedral (D_n) replication of a motif — which shipped with tests but no command. | `go run ./cmd/raymandala -fold 8 -mirror` |
| `raymeet` | The shared-world experience for a group: several people (and AIs) "call in" and walk the SAME 3-D world together, in the terminal, each seeing everyone else's avatar and able to tal… | `go run ./cmd/raymeet -host 5000` |
| `raymetaverse` | A real networked meeting INSIDE a shared world (Block F: metaverse over the wire). | `go run ./cmd/raymetaverse -n 3 -hexagram 110010` |
| `raypipe` | Shows the closed triangle end to end, offline and without a server: an AI brain authors a world from a prompt (2 — tvcp-ai/1), the world crosses the "wire" as MEANING — a compact S… | `go run ./cmd/raypipe "a calm dawn" "a vast cold desert"` |
| `rayplay` | Replays a recorded shared-world session (see raymeet -record): it reconstructs the world, the walkers and the chat over time from a tiny file of timestamped events — meaning, not pix… | `go run ./cmd/rayplay walk.rrec` |
| `rayquest` | Plays the Q6 hypercube as a roguelike (Block D). | `go run ./cmd/rayquest -seed 7 -walls 0.5` |
| `rayquestai` | An agent that PLAYS the Q6 roguelike (Block H: agent × maze). | `go run ./cmd/rayquestai -seed 16 -walls 0.6 -episodes 12` |
| `rayread` | Points the inverse reader at the real world (Block G: reader × vision). | `go run ./cmd/rayread` |
| `rayreading` | Casts an I-Ching reading and renders it as a world transition: the primary hexagram's world dissolves into voxel blocks and the relating hexagram's world forms from them (raydir.Rea… | `go run ./cmd/rayreading -seed 7` |
| `rayscore` | Writes the world's adaptive soundtrack to a WAV — activating raydir.ScoreWAV, which shipped with tests but no command. | `go run ./cmd/rayscore -mood calm -night 0.8 -out night.wav` |
| `rayseasons` | Runs a living world through the turning year (Block L: seasons). | `go run ./cmd/rayseasons -years 4 -year 120` |
| `raysound` | Gives the Q6 world a voice (Block J: sonification). | `go run ./cmd/raysound -hexagram 110010 -out world.wav` |
| `rayspectate` | Renders the raymeet shared world populated like an MMORPG: several player avatars and a swarm of patrolling robots in one world, shown both from a spectator's overhead view and thr… | `go run ./cmd/rayspectate` |
| `raystream` | A call where the wire carries scene DELTAS, not pixels: a sphere orbits a small world over many frames, each frame sent as a rayscene P-frame (raydir.SceneStream), and the receive… | `go run ./cmd/raystream -frames 60` |
| `rayterrain` | Activates the Delaunay terrain (raytrace.ScatterTerrain / DelaunayMesh / Delaunay) — shipped with tests but reachable from no command. | `go run ./cmd/rayterrain -seed 3 -amp 9` |
| `raytour` | Renders the Gray-code grand tour of all 64 hexagrams as cinema (Block I: tour of every world). | `go run ./cmd/raytour` |
| `rayview` | An interactive terminal navigator for the ray tracer. | `go run ./cmd/rayview` |
| `rayvoice` | Shows voice prosody steering the world's mood (the audio/reactive -> mood combination): three synthetic voice profiles — quiet, loud-and-steady, loud-and-swinging — are fed to a w… | `go run ./cmd/rayvoice` |
| `rayvoxel` | Renders a voxel-space landscape — the blocky height-field look the dream hackers describe as how dreams form ("Воксельная графика в сновидениях"). | `go run ./cmd/rayvoxel -seed 7 -out voxel.png` |
| `raywatch` | The camera observer of Part 1 (variant A): a camera frame of a machine goes through the same engine as a telemetry stream — visual signatures (heat, occlusion, glare, blur) are e… | `go run ./cmd/raywatch` |
| `rayworld` | Flies a ray-traced camera with a tvcp-ai brain and renders the result — neural directives -> camera -> real 3-D. | `go run ./cmd/rayworld` |
| `rayyard` | Renders the shared world as a robot yard: several robots patrol loops with status beacons green->red, and the world is rendered over time into a contact sheet — proof that robots are… | `go run ./cmd/rayyard` |
| `reliefdemo` | Renders tangram grid figures as 2.5D isometric reliefs (path 2): each glyph cell is extruded into a block, the diagonal glyphs becoming lit facets. | `go run ./cmd/reliefdemo` |
| `scenecall` | Animates a 3-D scene of tangram pieces (orbit + spin + bob), ships it as a SceneAnim (keyframe + deltas) over the Reed-Solomon channel, corrupts a contiguous burst, recovers, and ren… | `go run ./cmd/scenecall` |
| `semcall` | A REAL semantic video call: it sends a marks-spec stream (keyframes + semframe P-frames, each Reed-Solomon protected) over an actual localhost UDP socket under lossy conditions —… | `go run ./cmd/semcall [outdir]` |
| `semcodec` | Measures the rate-distortion of MEANING. | `go run ./cmd/semcodec` |
| `semdemo` | Shows semantic P-frames: a tiny "video" (a sun drifting across a sky grid) sent as one keyframe + per-frame deltas, and the byte savings vs sending every full frame. | `go run ./cmd/semdemo` |
| `semdistdemo` | Prints the rate-distortion-of-MEANING benchmark (pkg/semdist): how many wire bytes buy how much surviving meaning (MarksIoU), as the keyframe interval is varied under packet loss a… | `go run ./cmd/semdistdemo` |
| `semvideo` | The "closed loop": a semantic video call. | `go run ./cmd/semvideo [outdir]` |
| `sessiondemo` | Shows the unified game session (pkg/session): the same runner plays one Game (tic-tac-toe) between any Participants — here a tvcp-ai brain against another brain, then a brain aga… | `go run ./cmd/sessiondemo` |
| `solantype` | Shows two ways to set the myFront4Solan4 font: glyphs printed ADJACENT fuse their box borders into one continuous woven ornament/line, while a SPACE between them separates the letter… | `go run ./cmd/solantype [outdir]` |
| `streamdemo` | Shows the streaming tvcp-ai transport (pkg/stream): a brain PUSHES a sequence of responses over Server-Sent Events on one connection. | `go run ./cmd/streamdemo` |
| `tangram7demo` | Renders the AUTHENTIC seven-piece geometric tangram: the canonical square (verified to tile) and an exploded tray of all seven pieces. | `go run ./cmd/tangram7demo [outdir]` |
| `tangram7fig` | Renders the authored tangram7 figures (square, cat, swan) to PNGs and a combined menagerie sheet, printing each figure's verification. | `go run ./cmd/tangram7fig` |
| `tangrambench` | A spatial-reasoning benchmark for the tvcp-ai protocol. | `go run ./cmd/tangrambench [-brain URL] [-out dir]` |
| `tangramcraft` | A terminal "gate" prototype: you build a figure from the glyph ring while the AI (a pose-invariant classifier) names what you are becoming and reads your address; on completion… | `go run ./cmd/tangramcraft [-build cat] [-dial N]` |
| `tangramdemo` | Proves the tangram idea end to end: ONE small set of sub-cell pieces (◤◥◣◢ ▀▄▌▐ ▞▚ █) composes into many figures, every figure is checked against the tangram… | `go run ./cmd/tangramdemo [outdir]` |
| `tangramgallery` | Renders our figures as a framed "gallery wall": the catalog plus many procedural Creature(seed) figures, each in a wooden frame on a warm wall — the kind of geometric gallery… | `go run ./cmd/tangramgallery [out.png]` |
| `tangramhard` | Benchmarks silhouette-only ("hard") tangram solving: given just the figure's outline (no per-cell glyphs), recover the tiling. | `go run ./cmd/tangramhard` |
| `tangramnum` | Gives every tangram figure a unique NUMBER, the way the I Ching numbers its 64 hexagrams: read the figure's glyph grid row-major as a base-16 integer. | `go run ./cmd/tangramnum` |
| `tangramsong` | The third face of the project's "see, hear, summon" triad: it reads a tangram figure cell by cell (the same row-major reading that gives the figure its NUMBER), turns each cell… | `go run ./cmd/tangramsong` |
| `tangramzoo` | Renders the sixteen-figure tangram address book to a sheet and prints each figure's address and canonical id. | `go run ./cmd/tangramzoo` |
| `tools` | (see `go doc ./cmd/tools`) | `go run ./cmd/tools` |
| `tournament` | Runs a spectated tic-tac-toe LEAGUE (pkg/tournament over pkg/session): several participants — a minimax brain and a few scripted bots — play round-robin, home and away, and the… | `go run ./cmd/tournament` |
| `tvcp` | (see `go doc ./cmd/tvcp`) | `go run ./cmd/tvcp` |
| `unodemo` | Runs two reference brains through a full game of UNO on the session runner (pkg/session) — the first game on the frame with HIDDEN INFORMATION (each player sees only its own hand) an… | `go run ./cmd/unodemo` |
| `vocabdemo` | Shows the plural, localizable vocabulary (pkg/vocab): one canonical scene spoken in several languages. | `go run ./cmd/vocabdemo` |
| `watch` | The project's headline scenario, fused into ONE experience: two neural-or-reference commanders fight an arena match; the match is broadcast as a lossy SEMANTIC VIDEO CALL (pkg/arenaca… | `go run ./cmd/watch` |
| `wordledemo` | Shows the reference brain playing WORDLE on the session runner (pkg/session) — the first SOLITAIRE game on the same frame that runs tic-tac-toe and Connect Four. | `go run ./cmd/wordledemo` |
| `worldai` | Drives the 3-D world through the tvcp-ai protocol: each tick a brain receives a Brief of the world (game "world") and returns directives {fold,rise,spin,camera}; the world physics appl… | `go run ./cmd/worldai` |
| `worldloop` | Runs the closed loop: a Brain emits the next 51-byte world state each tick, the states ship as one deltastream over Reed-Solomon, a burst is injected and recovered, and every state i… | `go run ./cmd/worldloop` |
